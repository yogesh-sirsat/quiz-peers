import { Router } from "express";

const router = Router();

// router.get("/get-random-player-name", getRandomPlayerName);
// router.get("/check-player-name-exists", checkPlayerNameExists);

export default router;
